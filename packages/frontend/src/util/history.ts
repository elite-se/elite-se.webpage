import { createBrowserHistory } from 'history';

/*
 * This is the history object used by react router
 * set configuration options as appropriate
 * 
 * Having a custom history object permits easier/cleaner
 * manipulation, i.e., programatic navigation between pages
 */
export default createBrowserHistory({});
